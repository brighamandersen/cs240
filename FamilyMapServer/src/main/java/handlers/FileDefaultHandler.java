package handlers;

/**
 * Handles logic in deciding to which handler will take of the current query (based on the URL).
 */
public class FileDefaultHandler {
//    looks at current url
//            if the url is valid
//                sends it off to the correct handle
//            if url is invalid
//                redirect to 404 page




// WHAT HANDLERS DO
//    Handlers will create a request object, then call the corresponding service’s method and pass in the request.
}
